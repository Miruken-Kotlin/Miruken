package com.miruken.concurrent

/*
class PromiseActionTest {
    @Rule
    @JvmField val testName = TestName()

    class Foo

    @Test fun `Handles one argument action`() {
        val foo = Foo()
        assertAsync(testName) { done ->
            Promise.resolve(Handler().with(foo)) then { f: Foo ->
                assertSame(foo, f)
                done()
            }
        }
    }
}
*/